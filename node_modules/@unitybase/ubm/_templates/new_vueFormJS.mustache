<template>
<div class="u-form-layout">
  <u-toolbar />

  <u-form-container
    v-loading="loading"
    label-position="top"
  >
     <!-- place form layout here -->
    <u-auto-field attribute-name="attribute here" />
  </u-form-container>
</div>
</template>

<script>
const { Form, mapInstanceFields } = require('@unitybase/adminui-vue')
const { mapGetters, mapState } = require('vuex')
const UB = require('@unitybase/ub-pub')

module.exports.mount = function (cfg) {
  Form(cfg)
    .processing()
    .validation()
    .mount()
}

module.exports.default = {
  name: '{{className}}',
  inject: ['$v', 'entitySchema'],
  computed: {
    ...mapInstanceFields(['ID'/* add fields here */]),
    ...mapGetters(['loading']),
    ...mapState(['isNew'])
  },
  methods: {
    // someMethod () {
    //
    // }
  }
}
</script>
