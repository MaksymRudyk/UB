const FILE_NAME = Symbol('fileName')
const FILE_EXTENSION = Symbol('fileExtension')
const FILE_ENCODING = Symbol('encoding')
const FILE_CONTENT = Symbol('content')
const FILE_REL_PATH = Symbol('relPath')

module.exports = {
  FILE_NAME,
  FILE_ENCODING,
  FILE_CONTENT,
  FILE_EXTENSION,
  FILE_REL_PATH
}
